apiVersion: v1
kind: Service
metadata:
  name: oxigraph-cloud
  namespace: oxigraph-cloud
  labels:
    app.kubernetes.io/name: oxigraph-cloud
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 7878
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: oxigraph-cloud
